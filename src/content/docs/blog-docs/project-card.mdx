---
title: 'Project Card'
description: 'Showcase your work with style.'
order: 6
---
import ProjectCard from '~/components/blog/ProjectCard.astro';

The **ProjectCard** is the core of the portfolio section. It features a hover reveal effect for details and tags.

## Usage

```astro
import ProjectCard from '~/components/blog/ProjectCard.astro';

<ProjectCard
  title="Project Title"
  description="Brief description."
  image="https://example.com/image.jpg"
  tags={['React', 'Astro']}
  slug="project-detail"
/>
```

## Example

<div class="max-w-md mx-auto mb-8 p-6 border border-foreground/10 rounded-xl bg-background">
  <ProjectCard
    title="E-Commerce Redesign"
    description="A complete overhaul of a major retail platform focusing on conversion rate optimization."
    image="https://images.unsplash.com/photo-1661956602116-aa6865609028?w=800&q=80"
    tags={['UX Design', 'React', 'Shopify']}
    slug="#"
  />
</div>

## Props

| Prop | Type | Default | Description |
| :--- | :--- | :--- | :--- |
| `title` | `string` | Required | The name of the project. |
| `description` | `string` | Required | A brief overview of the project. |
| `image` | `string` | Required | URL of the project's cover image. |
| `tags` | `string[]` | Required | Array of technology or category tags. |
| `slug` | `string` | Required | URL slug for the project detail page. |
