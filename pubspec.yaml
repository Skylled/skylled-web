name: skylled_web
description: Kyle's personal website at skylled.dev, built with Jaspr.
version: 0.0.1

environment:
  sdk: ^3.10.0

dependencies:
  jaspr: ^0.22.4
  jaspr_content: ^0.5.1

dev_dependencies:
  build_runner: ^2.10.0
  build_web_compilers: ^4.4.17
  jaspr_builder: ^0.22.4
  lints: ^5.0.0

jaspr:
  mode: static
